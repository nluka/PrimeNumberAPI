const { test, expect } = require('@jest/globals');

test('getRandomPrimes() should return an array containing 1 random prime between 1 and 1,000,000 when no params are passed', () => {});

test('getRandomPrimes(count) should return an array containing 1 random prime between 1 and 1,000,000 when `count` is less than 1', () => {});

test('getRandomPrimes(count) should return an array of length 100 containing random primes between 1 and 1,000,000 when `count` is greater than 100', () => {});

test('getRandomPrimes(count) should return an array of length `count` containing random primes between 1 and 1,000,000 when `count` is between 1 and 100', () => {});
